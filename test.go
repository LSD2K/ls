/*package main

import (
	"fmt"
	"os"
)

func main() {
	var b float64 = 2
	switch b + 16 {
	case 16:
		fmt.Println("16")
	case 48:
		fmt.Printf("%v \n", b)
	default:
		fmt.Println("other")
	}
	for i := 1; i < 10; i++ {
		for j := 1; j < 10; j++ {
			if (i*j)%10 == 0 {
				fmt.Print("\t")
				continue
			}
			fmt.Print(i*j, "\t")
		}
		fmt.Println()
	}
	var user = [5]string{"tom", "alisa", "macks", "nikita", "anton"}
	for index, value := range user {

		fmt.Printf("index: %d, value: %s\n", index, value)
	}
	for i := 0; i < len(user); i++ {
		fmt.Printf("index: %d, value: %s \n", i, user[i])
	}
	fmt.Println(add(10, 20))
}
func add(x, y int) int {

	return x + y
}*/
